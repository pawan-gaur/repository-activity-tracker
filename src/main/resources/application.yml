server:
  port: 8080

github:
  base-url: https://api.github.com
  token: ${GITHUB_TOKEN:}   # set env var or put token here (not recommended)

management:
  endpoints:
    web:
      exposure:
        include: health,info
